# Build Command
npm run build

# Output Directory
dist

# Environment Variables
VITE_API_URL=https://your-backend-app.railway.app

# Rewrites (for API proxy in production)
# Configure in vercel.json - already set up to proxy /api/* to your backend