# Node.js Runtime
Node.js 22.x

# Build Command
npm install

# Start Command
npm start

# Environment Variables
# Add these in Railway dashboard or via CLI:
# ANTHROPIC_API_KEY=sk-ant-xxxxx
# MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/chatgpt-like
# CLIENT_URL=https://your-frontend.vercel.app

# Health Check
GET / will return 200 if server is running (add a health check route if needed)